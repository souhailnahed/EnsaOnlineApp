<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ensaonline6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_ensaonlinelogoe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ensaonlinelogoe"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnsaMaroc"></activity>
        <activity android:name=".CycleIng" />
        <activity android:name=".CyclePrepa" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>